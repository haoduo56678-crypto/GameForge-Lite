{
  "name": "gameforge-lite",
  "version": "2.1.1",
  "private": true,
  "description": "Zero-cost Minecraft Java 1.20.1 creation studio",
  "scripts": {
    "build": "node scripts/check-core-package.js && node scripts/build-release.js && node scripts/check-jar-builder.js && node scripts/check-core-mechanisms.js",
    "start": "npm run build && node scripts/serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
